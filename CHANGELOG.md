# 更新日志

本项目遵循 [语义化版本](https://semver.org/lang/zh-CN/)；格式参考 Keep a Changelog。

## [Unreleased]

### 变更 — 品牌

- **产品更名：FreeTool → 兰台（Lantai）**。兰台为汉代典籍藏书之所，档案系统雅称——与本产品「文书、台账、报送材料整理归档」的定位一致。
- 更名波及：应用名 / 窗口标题 / 托盘 / 通知前缀 / appId（`com.ruai1024.lantai`）/ 安装器与卸载项显示名 / 产物命名（`lantai-setup-*.exe`、`lantai-portable-*.exe`）。
- 可执行文件取 ASCII 名 `Lantai.exe`，规避中文安装路径在个别企业环境的兼容问题。
- 数据目录随应用名变更为 `%APPDATA%\兰台`（开发版 `%APPDATA%\lantai`），**首次启动自动从旧 `%APPDATA%\FreeTool` 迁移**设置、历史与窗口状态，旧文件原样保留。
- 品牌字（侧栏 logo、首页大标题）改用仿宋——公文正文字体，气质对齐「文书档案」。

### 变更 — 更新体验

- 官方更新源内置（`ruai0/Lantai` Releases），用户零配置开箱即用；「软件更新」卡片去掉技术术语，更新源输入折叠为「更新源设置（一般无需修改）」。
- 内网机器可在更新源里填镜像目录；填 `off` 彻底禁用一切更新请求。

### 新增 — 常驻与品牌

- 关闭主窗口行为可配置：每次询问（默认，弹「最小化到托盘 / 退出」选择框可记住）/ 收进托盘 / 直接退出；托盘左键恢复、右键菜单含「显示主窗口 / 检查更新 / 退出 兰台」，检查结果以系统通知反馈。
- 隐藏调试面板：界面任意处连续键入 8 字母口令弹出，四个标签页——环境键值 / 主日志尾巴 / 原始 settings+history / 操作（DevTools、立即检查更新、打开主页、重启、重置设置）。
- 作者署名上界面：侧栏版本号旁、首页页脚、设置「关于」行均显示 © 2026 ruai0 与项目主页链接（点击用默认浏览器打开）。
- 新用户首启三步指引（离线承诺 → 基本用法 → 更新与反馈），完成或跳过后不再出现。

## [0.5.0] - 2026-09-12

### 新增 — 平台层

- 工具注册表：侧栏 / 首页 / 命令面板 / 收藏共用单一数据源
- 命令面板（Ctrl+K）：按关键词直达工具、搜索最近产出、快捷操作（主题切换等）
- 工具收藏：首页置顶分组 + 侧栏「收藏」分组
- 系统托盘：常驻图标、关闭窗口后从托盘唤回、二次启动聚焦已有窗口
- 完成通知：窗口不在前台时任务完成弹 Windows 通知，点击唤回主窗口
- 任务队列与进度坞：重批量任务（合并 PDF / Office 转 PDF / 批量替换 / ZIP 打包）后台串行执行、按文件实时进度、右下角悬浮任务坞
- 环境探测：检测本机 Word / Excel / PPT 的 Microsoft / WPS 组件与中文字体，展示在设置页并计入诊断信息
- 拖入整个文件夹自动展开（按各工具支持类型过滤，2000 个封顶）
- 批量重命名 / 归类一键撤销：保留最近一次执行，异常中止也可恢复
- 一键复制诊断信息（版本 / 系统 / 日志 / 环境探测），报障免截图
- 软件更新（electron-updater）：更新源可配置（自建静态目录或 GitHub Releases），未配置时零网络请求
- 深色模式、默认输出目录、使用历史、窗口记忆

### 发布工程

- NSIS 安装包（中文安装向导）+ 便携版，产物命名 `freetool-setup-<版本>.exe` / `freetool-portable-<版本>.exe`
- `npm run release <版本>`：版本号 → 提交 → git tag → 构建 → SHA256SUMS.txt 一键完成
- LICENSE（MIT，© 2026 ruai1024）；exe 内嵌版权信息
